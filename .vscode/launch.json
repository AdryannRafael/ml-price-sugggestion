{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Executar Script: dev",
      "request": "launch",
      "command": "npm run dev",
      // "command": "npm run build && npm run preview",
      "cwd": "${workspaceFolder}"
    }
  ]
}
